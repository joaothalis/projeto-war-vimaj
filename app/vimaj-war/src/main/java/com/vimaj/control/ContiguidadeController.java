package com.vimaj.control;

import java.util.List;

import com.vimaj.model.Territorio;
import com.vimaj.model.Contiguidade;

public class ContiguidadeController {
	/*List<Contiguidade> contiguidade;
	
	public boolean Verificarcontiguidade(Territorio a, Territorio b) {
		String inicial = a.getNome();
		String fronteira = b.getNome();
		for (int i = 0; i < vertices.size(); i++) {
			if (vertices.get(i).getNome().equals(inicial)) {
				for (int j = 0; j < arestas.size(); j++) {
					if (vertices.get(i).getAdj().get(j).getDestino().getNome().equals(fronteira)) {
						return true;
					}
				}
			}
		}
		return false;
	}*/
}
